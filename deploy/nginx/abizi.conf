server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    root /var/www/abizi/current;
    index index.html;
    charset utf-8;
    server_tokens off;

    add_header X-Robots-Tag "noindex, nofollow" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    location ~ /\.(?!well-known) {
        deny all;
    }

    error_page 404 /404.html;
}

server {
    listen 80;
    listen [::]:80;
    server_name abizi.ru www.abizi.ru;

    return 301 https://abizi.ru$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name www.abizi.ru;

    ssl_certificate /etc/letsencrypt/live/abizi.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/abizi.ru/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    return 301 https://abizi.ru$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name abizi.ru;

    root /var/www/abizi/current;
    index index.html;
    charset utf-8;
    server_tokens off;
    client_max_body_size 1m;

    ssl_certificate /etc/letsencrypt/live/abizi.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/abizi.ru/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    add_header Strict-Transport-Security "max-age=31536000" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/javascript application/json application/xml image/svg+xml;

    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    location /_next/static/ {
        expires 1y;
        try_files $uri =404;
    }

    location ~* \.(?:png|jpe?g|gif|svg|webp|ico|avif|woff2?)$ {
        expires 7d;
        try_files $uri =404;
    }

    location ~ /\.(?!well-known) {
        deny all;
    }

    error_page 404 /404.html;
}
